.git
.gitignore
.env
.env.local
.env.production
.env*.local
.claude
node_modules
.next
*.tsbuildinfo
next-env.d.ts
README.md
docs/
tests/
# Design reference only. Anything under public/ is served as a static asset, so
# leaving these in would publish the component source at /ui/orbReference/*.
public/ui/orbReference
Dockerfile
.dockerignore

# Source material for the bake, not runtime assets. public/reveal/fathom-surfboard.jpg
# is what ships; the 1.7MB original does not need to. The order guide is an
# internal Ford document and has no business inside a deployed container.
app/*.doc
app/*.docx
app/Fathom w surfboard mobile.png
