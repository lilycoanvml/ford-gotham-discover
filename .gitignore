# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/

# Environment variables (never commit real secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build output
dist/
build/

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Design reference / prototype files (not production code)
*.zip

# Source material, not deliverables. public/reveal/fathom-surfboard.jpg is the
# baked copy that ships; the 1.7MB original stays local. The order guide is an
# internal Ford document and must not be committed — its facts live in
# REVEAL_PROMPT, the file itself does not belong in the repo.
app/*.doc
app/*.docx
app/Fathom w surfboard mobile.png
